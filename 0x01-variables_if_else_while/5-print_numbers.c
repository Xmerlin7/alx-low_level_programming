#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry point
 * return: 0
 */
int main(void)
{
	int n;

	while (n <= 9)
	{
		ptintf("%d", n);
		n++;
	}
	printf("\n");
	return (0);
}
